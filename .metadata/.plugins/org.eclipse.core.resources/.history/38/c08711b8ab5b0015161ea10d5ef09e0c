package BufferSincronizadoAlmacenado;

import java.util.Scanner;
import java.util.concurrent.Semaphore;

public class Main {
	

	final static int valoresMax = 10;

	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		Semaphore barreraLeido = new Semaphore(valoresMax);
		Semaphore barreraEscrito = new Semaphore(0);
		Buffer buffer = new Buffer(barreraEscrito, barreraLeido);
		Productor productor = new Productor(buffer);
		Consumidor consumidor = new Consumidor(buffer);

		productor.start();
		consumidor.start();
		
		scanner.nextLine();
		
		productor.stop();
		consumidor.stop();
		
	}

}
